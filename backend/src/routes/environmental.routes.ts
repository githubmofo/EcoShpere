// src/routes/environmental.routes.ts
import { Router } from "express";
// import { EnvironmentalController } from "../controllers/environmental.controller";

const router = Router();

// TODO: Define environmental routes
// router.get("/emission-factors", EnvironmentalController.getEmissionFactors);
// router.get("/carbon-transactions", EnvironmentalController.getCarbonTransactions);
// router.get("/goals", EnvironmentalController.getGoals);

export default router;
